---
import { useTranslations, type Lang } from "../i18n/util";
import { Icon } from "astro-icon/components";

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
const otherLang: Lang = lang === "en" ? "fr" : "en";
const otherHref = otherLang === "fr" ? "/fr/" : "/";

const pathname = Astro.url.pathname;
const isHomepage = pathname === "/" || pathname === "/fr/";

const navItems = [
  { index: "01", key: "services", href: lang === "fr" ? "/fr/services" : "/services" },
  { index: "02", key: "work", href: isHomepage ? "#proof" : (lang === "fr" ? "/fr/portfolio" : "/portfolio") },
  { index: "03", key: "team", href: isHomepage ? "#team" : (lang === "fr" ? "/fr/team" : "/team") },
  { index: "04", key: "faq", href: isHomepage ? "#faq" : (lang === "fr" ? "/fr/#faq" : "/#faq") },
  { index: "05", key: "contact", href: isHomepage ? "#contact" : (lang === "fr" ? "/fr/#contact" : "/#contact") },
] as const;
---

<header
  id="navbar"
  class="fixed top-0 z-50 w-full transition-all duration-400 ease-[cubic-bezier(0.22,1,0.36,1)]"
  style="background: rgba(11,34,51,0.75); backdrop-filter: blur(14px); -webkit-backdrop-filter: blur(14px); border-bottom: 1px solid rgba(25,227,240,0.08);"
>
  <!-- Scroll progress bar (top edge) -->
  <div id="scroll-progress" class="absolute top-0 left-0 h-[2px] z-10" style="width: 0%; background: linear-gradient(90deg, var(--color-accent), var(--color-ember));"></div>

  <!-- Inner container -->
  <div class="header-inner flex items-center justify-between px-[var(--margin-mobile)] md:px-[var(--margin-desktop)]" style="max-width: var(--container-max); margin: 0 auto; height: 80px; transition: height 0.4s cubic-bezier(0.22,1,0.36,1);">
    <!-- Left: Logo + Wordmark -->
    <a href={lang === "fr" ? "/fr/" : "/"} class="flex items-center gap-2 sm:gap-3 no-underline group flex-shrink-0">
      <img src="/logo-96.png" alt="Flintlock" class="header-logo h-10 sm:h-11 w-auto object-contain flex-shrink-0 transition-all duration-400 ease-[cubic-bezier(0.22,1,0.36,1)]" width="96" height="96" fetchpriority="high" decoding="async" />
      <span class="font-display text-sm sm:text-base md:text-lg font-extrabold tracking-[0.15em] text-[var(--color-off-white)] whitespace-nowrap header-wordmark transition-all duration-400">FLINTLOCK</span>
    </a>

    <!-- Center: Desktop nav -->
    <nav class="hidden md:flex items-center gap-5 lg:gap-7 ml-auto mr-4 lg:mr-6" aria-label="Main navigation">
      {navItems.map((item) => (
        <a
          href={item.href}
          class="nav-link relative font-mono text-[11px] tracking-[0.15em] uppercase no-underline text-[var(--color-text-secondary)] hover:text-[var(--color-accent)] transition-colors duration-300 py-2"
        >
          <span class="font-medium">{t(`nav.${item.key}`)}</span>
          <span class="nav-underline" aria-hidden="true"></span>
        </a>
      ))}
    </nav>

    <!-- Right: Available pill + Lang toggle + CTA -->
    <div class="flex items-center gap-2 sm:gap-3 lg:gap-4 flex-shrink-0">
      <!-- Green "Available" pill (desktop) -->
      <div class="hidden lg:flex items-center gap-2 px-3 py-1.5 rounded-full border border-green-500/15 bg-green-500/5">
        <span class="relative flex w-1.5 h-1.5">
          <span class="absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-40 motion-safe:animate-ping motion-safe:animation-duration-[2s]"></span>
          <span class="relative inline-flex rounded-full h-1.5 w-1.5 bg-green-400"></span>
        </span>
        <span class="font-mono text-[10px] tracking-wide text-[var(--color-text-secondary)] whitespace-nowrap">{t("nav.status")}</span>
      </div>

      <!-- EN/FR toggle switch -->
      <button
        id="lang-switch"
        role="switch"
        aria-checked={lang === "fr" ? "true" : "false"}
        aria-label={lang === "en" ? t("a11y.switchToFr") : t("a11y.switchToEn")}
        data-other-href={otherHref}
        data-persist="true"
        class="lang-toggle relative flex items-center gap-1 px-2.5 sm:px-3 py-1.5 sm:py-2 rounded-full border border-[var(--color-border)] bg-[var(--onyx)]/60 cursor-pointer transition-colors duration-300 hover:border-[var(--color-accent)]/30 focus-visible:outline-2 focus-visible:outline-[var(--color-accent)]"
        style="min-width: 56px;"
      >
        <span class={`font-mono text-[10px] font-bold tracking-wider transition-colors duration-300 ${lang === "en" ? "text-[var(--color-accent)]" : "text-[var(--color-text-secondary)]/40"}`}>EN</span>
        <span class="lang-knob relative w-4 h-4 rounded-full transition-transform duration-300" style="background: var(--color-accent); transform: translateX(0);"></span>
        <span class={`font-mono text-[10px] font-bold tracking-wider transition-colors duration-300 ${lang === "fr" ? "text-[var(--color-accent)]" : "text-[var(--color-text-secondary)]/40"}`}>FR</span>
      </button>

      <!-- CTA: Book a call (desktop) -->
      <a
        href="https://wa.me/212706570686"
        target="_blank"
        rel="noopener"
        class="ignition-cta hidden md:inline-flex items-center gap-2 px-5 py-2.5 no-underline font-bold text-[11px] uppercase tracking-[0.15em] rounded-[var(--radius-md)] relative overflow-hidden group"
        style="background: var(--color-cta); color: #000;"
      >
        <span class="relative z-10 flex items-center gap-2">
          <Icon name="lucide:phone" class="w-3.5 h-3.5" />
          <span>{t("nav.book")}</span>
        </span>
        <span class="cta-glow absolute inset-0 pointer-events-none opacity-0 group-hover:opacity-100 transition-opacity duration-500" aria-hidden="true"></span>
      </a>

      <!-- Hamburger (mobile) -->
      <button
        id="mobile-menu-btn"
        class="flex md:hidden hamburger-btn relative bg-transparent border-none cursor-pointer p-2 sm:p-2.5 transition-transform duration-200 active:scale-90"
        style="touch-action: manipulation; min-width: 44px; min-height: 44px; align-items: center; justify-content: center;"
        aria-label={t("a11y.openMenu")}
        aria-controls="mobile-menu"
        aria-expanded="false"
      >
        <span class="hamburger-line hamburger-top absolute w-5 sm:w-6 h-[1.5px] bg-[var(--color-off-white)] transition-all duration-300 ease-[cubic-bezier(0.22,1,0.36,1)]" style="top: calc(50% - 5px);"></span>
        <span class="hamburger-line hamburger-mid absolute w-5 sm:w-6 h-[1.5px] bg-[var(--color-off-white)] transition-all duration-300 ease-[cubic-bezier(0.22,1,0.36,1)]" style="top: 50%;"></span>
        <span class="hamburger-line hamburger-bot absolute w-5 sm:w-6 h-[1.5px] bg-[var(--color-off-white)] transition-all duration-300 ease-[cubic-bezier(0.22,1,0.36,1)]" style="top: calc(50% + 5px);"></span>
      </button>
    </div>
  </div>
</header>

<!-- Mobile full-screen overlay -->
<div
  id="mobile-menu"
  class="fixed inset-0 z-[60] flex flex-col items-center justify-center md:hidden transition-all duration-400 ease-[cubic-bezier(0.22,1,0.36,1)]"
  style="background: rgba(11,34,51,0.96); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); clip-path: circle(0% at calc(100% - 40px) 32px);"
  aria-label={t("a11y.mobileNav")}
  role="dialog"
  aria-modal="true"
  hidden
>
  <!-- Blueprint grid background -->
  <div class="absolute inset-0 pointer-events-none" aria-hidden="true" style="background-image: linear-gradient(rgba(25,227,240,0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(25,227,240,0.03) 1px, transparent 1px); background-size: 40px 40px;"></div>

  <!-- Close button -->
  <button
    id="mobile-menu-close"
    class="absolute top-5 right-5 text-[var(--color-off-white)] bg-[var(--color-surface-2)]/50 border border-[var(--color-border)] rounded-full cursor-pointer p-3 z-10 transition-transform duration-200 active:scale-90"
    style="touch-action: manipulation; min-width: 44px; min-height: 44px;"
    aria-label={t("a11y.closeMenu")}
  >
    <Icon name="lucide:x" class="w-6 h-6" />
  </button>

  <!-- Mobile content -->
  <div class="flex flex-col items-center gap-8 relative z-[1] w-full max-w-sm px-6">
    <!-- Logo area -->
    <div class="flex flex-col items-center gap-3 mb-4 mobile-stagger">
      <img src="/logo-96.png" alt="Flintlock" class="w-16 h-16 object-contain" width="96" height="96" decoding="async" />
      <span class="font-display text-lg font-extrabold tracking-[0.15em] text-[var(--color-off-white)]">FLINTLOCK</span>
    </div>

    <!-- Mobile nav links -->
    <nav class="flex flex-col items-center gap-1" aria-label={t("a11y.mobileNav")}>
      {navItems.map((item, i) => (
        <a
          href={item.href}
          class="mobile-nav-link flex items-center gap-4 no-underline py-3 px-4 w-full max-w-xs rounded-lg hover:bg-white/5 transition-colors duration-200 mobile-stagger"
          style={`transition-delay: ${0.08 * i}s`}
        >
          <span class="font-mono text-[10px] tracking-wider text-[var(--color-accent)]/50 w-5 text-right flex-shrink-0">{item.index}</span>
          <span class="font-mono text-base tracking-[0.15em] uppercase text-[var(--color-off-white)] hover:text-[var(--color-accent)] transition-colors duration-300">{t(`nav.${item.key}`)}</span>
        </a>
      ))}
    </nav>

    <!-- Green Available pill (mobile) -->
    <div class="flex items-center gap-2 px-5 py-3 rounded-full border border-green-500/15 mobile-stagger" style="transition-delay: 0.4s;">
      <span class="relative flex w-2 h-2">
        <span class="absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-40 motion-safe:animate-ping motion-safe:animation-duration-[2s]"></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-green-400"></span>
      </span>
      <span class="font-mono text-xs tracking-wide text-[var(--color-text-secondary)]">{t("nav.status")}</span>
    </div>

    <!-- Lang toggle (mobile) -->
    <button
      role="switch"
      aria-checked={lang === "fr" ? "true" : "false"}
      aria-label={lang === "en" ? t("a11y.switchToFr") : t("a11y.switchToEn")}
      data-other-href={otherHref}
      data-persist="true"
      class="lang-toggle flex items-center gap-2 px-4 py-3 rounded-full border border-[var(--color-border)] bg-[var(--onyx)]/60 cursor-pointer mobile-stagger"
      style="transition-delay: 0.48s;"
    >
      <span class={`font-mono text-xs font-bold tracking-wider transition-colors duration-300 ${lang === "en" ? "text-[var(--color-accent)]" : "text-[var(--color-text-secondary)]/40"}`}>EN</span>
      <span class="w-5 h-5 rounded-full transition-transform duration-300" style="background: var(--color-accent);"></span>
      <span class={`font-mono text-xs font-bold tracking-wider transition-colors duration-300 ${lang === "fr" ? "text-[var(--color-accent)]" : "text-[var(--color-text-secondary)]/40"}`}>FR</span>
    </button>

    <!-- CTA: Book a call (mobile) -->
    <a
      href="#"
      data-booking-trigger
      class="mobile-nav-link inline-flex items-center gap-2.5 px-8 py-4 no-underline font-bold text-sm uppercase tracking-[0.15em] rounded-[var(--radius-md)] mobile-stagger relative overflow-hidden group"
      style="background: var(--color-cta); color: #000; transition-delay: 0.56s;"
    >
      <Icon name="lucide:phone" class="w-4 h-4" />
      <span>{t("nav.book")}</span>
      <span class="cta-glow absolute inset-0 pointer-events-none opacity-0 group-hover:opacity-100 transition-opacity duration-500"></span>
    </a>
  </div>
</div>

<style is:global>
  /* ─── Nav link underline + active ─── */
  .nav-link .nav-underline {
    position: absolute;
    bottom: -3px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--color-accent);
    transition: width 0.35s cubic-bezier(0.22, 1, 0.36, 1);
  }
  .nav-link:hover .nav-underline,
  .nav-link.active .nav-underline {
    width: 100%;
  }
  .nav-link.active {
    color: var(--color-accent);
  }

  /* ─── Ignition CTA heat glow ─── */
  .cta-glow {
    background: radial-gradient(ellipse 60% 50% at 50% 50%, rgba(255, 122, 26, 0.5), transparent 70%);
  }
  .ignition-cta:hover {
    box-shadow: 0 0 20px rgba(255, 122, 26, 0.5), 0 0 40px rgba(255, 122, 26, 0.2);
  }

  /* ─── EN/FR lang toggle ─── */
  .lang-toggle[aria-checked="true"] .lang-knob {
    transform: translateX(10px);
  }
  .lang-toggle[aria-checked="false"] .lang-knob {
    transform: translateX(0px);
  }

  /* ─── Hamburger → X morph ─── */
  .hamburger-btn[aria-expanded="true"] .hamburger-top {
    top: 50% !important;
    transform: translateY(-50%) rotate(45deg);
  }
  .hamburger-btn[aria-expanded="true"] .hamburger-mid {
    opacity: 0;
    transform: scaleX(0);
  }
  .hamburger-btn[aria-expanded="true"] .hamburger-bot {
    top: 50% !important;
    transform: translateY(-50%) rotate(-45deg);
  }

  /* ─── Mobile overlay open/close ─── */
  #mobile-menu[hidden] {
    display: none !important;
  }
  #mobile-menu.open {
    clip-path: circle(150% at 50% 50%);
  }

  /* ─── Mobile stagger animation ─── */
  #mobile-menu.open .mobile-stagger {
    opacity: 0;
    transform: translateY(16px);
    animation: stagger-in 0.5s cubic-bezier(0.22, 1, 0.36, 1) forwards;
  }
  @keyframes stagger-in {
    to { opacity: 1; transform: translateY(0); }
  }

  /* ─── Scrolled shrink state (desktop only) ─── */
  @media (min-width: 768px) {
    #navbar.scrolled .header-inner {
      height: 60px;
    }
    #navbar.scrolled .header-logo {
      height: 2rem;
    }
    #navbar.scrolled .header-wordmark {
      font-size: 0.875rem;
      letter-spacing: 0.12em;
    }
    #navbar.scrolled {
      width: 92%;
      max-width: 1060px;
      left: 50%;
      transform: translateX(-50%);
      border-radius: 16px;
      background: rgba(11,34,51,0.88);
      backdrop-filter: blur(20px) saturate(1.4);
      -webkit-backdrop-filter: blur(20px) saturate(1.4);
      border: 1px solid rgba(25,227,240,0.08);
      box-shadow:
        0 8px 32px rgba(0,0,0,0.35),
        inset 0 0 0 1px rgba(255,255,255,0.04);
      margin-top: 8px;
    }
    #navbar.scrolled.hidden-nav {
      transform: translateX(-50%) translateY(-120%) !important;
    }
  }

  /* ─── Hide on scroll down / show on scroll up ─── */
  #navbar.hidden-nav {
    transform: translateY(-120%) !important;
  }
  #navbar.scrolled.hidden-nav {
    transform: translateX(-50%) translateY(-120%) !important;
  }

  /* ─── Reduced motion ─── */
  @media (prefers-reduced-motion: reduce) {
    .cta-glow { display: none; }
    .ignition-cta:hover { box-shadow: none; }
    .nav-link .nav-underline { transition: none; }
    .nav-link:hover .nav-underline { width: 0; }
    #navbar { transition: none; }
    #navbar.scrolled { transition: none; }
    #mobile-menu { transition: none; }
    #mobile-menu.open .mobile-stagger {
      opacity: 1;
      transform: none;
      animation: none;
    }
    .lang-toggle .lang-knob { transition: none; }
    .hamburger-line { transition: none; }
    #scroll-progress { display: none; }
  }
</style>

<script>
  (function () {
    function initHeader() {
      var navbar = document.getElementById("navbar");
      var menuBtn = document.getElementById("mobile-menu-btn");
      var menuClose = document.getElementById("mobile-menu-close");
      var menu = document.getElementById("mobile-menu");
      var langToggles = document.querySelectorAll(".lang-toggle");
      var mobileNavLinks = document.querySelectorAll(".mobile-nav-link");

      if (!navbar) return;

      var scrollProgress = document.getElementById("scroll-progress");
      var ticking = false;
      var lastScroll = 0;
      var scrollThreshold = 50;
      var hideThreshold = 100;
      var isScrolled = false;

      /* ─── Scroll handler ─── */
      function onScroll() {
        var scrollY = window.scrollY;

        /* Morph to capsule at threshold */
        if (scrollY > scrollThreshold && !isScrolled) {
          navbar.classList.add("scrolled");
          isScrolled = true;
        } else if (scrollY <= scrollThreshold && isScrolled) {
          navbar.classList.remove("scrolled");
          navbar.classList.remove("hidden-nav");
          isScrolled = false;
        }

        /* Hide on scroll down / show on scroll up */
        if (scrollY > hideThreshold) {
          if (scrollY > lastScroll) {
            navbar.classList.add("hidden-nav");
          } else {
            navbar.classList.remove("hidden-nav");
          }
        } else {
          navbar.classList.remove("hidden-nav");
        }

        /* Update scroll progress bar */
        if (scrollProgress) {
          var docHeight = document.documentElement.scrollHeight - window.innerHeight;
          var pct = docHeight > 0 ? (scrollY / docHeight) * 100 : 0;
          scrollProgress.style.width = pct + "%";
        }

        lastScroll = scrollY;
        ticking = false;
      }

      /* ─── Scroll-spy: highlight active nav section ─── */
      var sections = document.querySelectorAll("section[id]");
      var navLinks = document.querySelectorAll(".nav-link");
      if (sections.length && navLinks.length) {
        var sectionObs = new IntersectionObserver(function (entries) {
          entries.forEach(function (entry) {
            if (entry.isIntersecting) {
              var id = entry.target.id;
              navLinks.forEach(function (link) {
                link.classList.toggle("active", link.getAttribute("href") === "#" + id);
              });
            }
          });
        }, { rootMargin: "-40% 0px -55% 0px" });
        sections.forEach(function (s) { sectionObs.observe(s); });
      }

      /* ─── Path-based active state for subpages ─── */
      var currentPath = window.location.pathname.replace(/\/$/, "");
      navLinks.forEach(function (link) {
        var href = link.getAttribute("href") || "";
        var linkPath = href.replace(/\/$/, "");
        if (linkPath && linkPath !== "#" && currentPath === linkPath) {
          link.classList.add("active");
        }
      });

      window.addEventListener("scroll", function () {
        if (!ticking) {
          requestAnimationFrame(onScroll);
          ticking = true;
        }
      }, { passive: true });

      /* ─── Lang toggle ─── */
      langToggles.forEach(function (toggle) {
        toggle.addEventListener("click", function () {
          var href = this.getAttribute("data-other-href");
          if (href) {
            localStorage.setItem("flintlock-lang", href === "/fr/" ? "fr" : "en");
            window.location.href = href;
          }
        });

        toggle.addEventListener("keydown", function (e) {
          if (e.key === "Enter" || e.key === " ") {
            e.preventDefault();
            this.click();
          }
        });
      });

      /* ─── Mobile menu ─── */
      var lastFocused = null;

      function openMenu() {
        if (!menu || !menuBtn) return;
        lastFocused = document.activeElement;
        menu.removeAttribute("hidden");
        void menu.offsetHeight;
        menu.classList.add("open");
        menuBtn.setAttribute("aria-expanded", "true");
        menuBtn.setAttribute("aria-label", window.location.pathname.startsWith("/fr") ? "Fermer le menu" : "Close menu");
        document.body.style.overflow = "hidden";
        if (menuClose) menuClose.focus();
      }

      function closeMenu() {
        if (!menu || !menuBtn) return;
        menu.classList.remove("open");
        menuBtn.setAttribute("aria-expanded", "false");
        menuBtn.setAttribute("aria-label", window.location.pathname.startsWith("/fr") ? "Ouvrir le menu" : "Open menu");
        document.body.style.overflow = "";
        setTimeout(function () {
          menu.setAttribute("hidden", "");
          if (lastFocused) lastFocused.focus();
        }, 450);
      }

      if (menuBtn && menu) {
        menuBtn.addEventListener("click", openMenu);

        if (menuClose) {
          menuClose.addEventListener("click", closeMenu);
        }

        mobileNavLinks.forEach(function (link) {
          link.addEventListener("click", closeMenu);
        });

        document.addEventListener("keydown", function (e) {
          if (e.key === "Escape" && menu.classList.contains("open")) {
            closeMenu();
          }
        });

        /* Focus trap */
        menu.addEventListener("keydown", function (e) {
          if (e.key !== "Tab") return;
          var focusable = menu.querySelectorAll('button, a, [tabindex]:not([tabindex="-1"])');
          if (focusable.length === 0) return;
          var first = focusable[0];
          var last = focusable[focusable.length - 1];
          if (e.shiftKey && document.activeElement === first) {
            e.preventDefault();
            last.focus();
          } else if (!e.shiftKey && document.activeElement === last) {
            e.preventDefault();
            first.focus();
          }
        });
      }

      /* ─── Magnetic pull on nav links (desktop) ─── */
      var prefersReduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
      if (!prefersReduced) {
        var navLinksMagnetic = document.querySelectorAll(".nav-link");
        navLinksMagnetic.forEach(function (link) {
          link.addEventListener("mousemove", function (e) {
            var rect = link.getBoundingClientRect();
            var x = e.clientX - rect.left - rect.width / 2;
            var y = e.clientY - rect.top - rect.height / 2;
            link.style.transform = "translate(" + (x * 0.12) + "px, " + (y * 0.12) + "px)";
          });
          link.addEventListener("mouseleave", function () {
            link.style.transform = "translate(0px, 0px)";
          });
        });
      }
    }

    if (document.readyState === "loading") {
      document.addEventListener("DOMContentLoaded", initHeader);
    } else {
      initHeader();
    }
  })();
</script>
